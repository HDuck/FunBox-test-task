- 
    let koteCards = [
        {
            status: "default",
            filler: "с фуа-гра",
            amount: 10,
            promoRats: 1,
            mood: false,
            weight: 0.5
        },
        {
            status: "selected",
            filler: "с рыбой",
            amount: 40,
            promoRats: 2,
            mood: false,
            weight: 2
        },
        {
            status: "disabled",
            filler: "с курой",
            amount: 100,
            promoRats: 5,
            mood: true,
            weight: 5
        }
    ];

include kote-food.pug

<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title FunBox-test-task
        link(rel="stylesheet", href="./css/style.css")
    body
        .container
            each val in koteCards
                +kote(val.status, val.filler, val.amount, val.promoRats, val.mood, val.weight)